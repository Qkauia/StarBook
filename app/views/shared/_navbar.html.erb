<nav
  class="flex items-center justify-between px-3 py-2 text-white bg-slate-800"
>
  <div class="flex items-center gap-2">
    <h1 class="text-2xl font-bold"><%= link_to "閃亮書店",root_path %></h1>
    <ul class="flex gap-3">
      <li><%= link_to "書籍列表", books_path %></li>
      <% if user_signed_in? %>
      <li><%= link_to "新增書籍", new_book_path %></li>
      <% end %>
    </ul>
  </div>
  <div>
    <ul class="flex gap-3">
      <% if user_signed_in? %>
        <li><%= current_user.email %></li>
        <%= link_to '登出', sessions_path, data: { turbo_method: 'delete' } %>
      <% else %>
        <li><%= link_to '註冊', new_users_path %></li>
        <li><%= link_to '登入', sign_in_users_path %></li>
      <% end %>
    </ul>
  </div>
</nav>